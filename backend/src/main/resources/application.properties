# Server Configuration
# This allows the backend to accept connections from your Android Emulator/Phone
server.address=0.0.0.0
server.port=8080

# Database Connection 
spring.datasource.url=jdbc:postgresql://db.atlxlghgfzscgyrlbzhd.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=123Qqwewsaw3
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Explicitly set the dialect to prevent the "Unable to determine Dialect" error
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Connection Pool Settings
# Hikari settings to handle Supabase connection stability
spring.datasource.hikari.initialization-fail-timeout=0
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000